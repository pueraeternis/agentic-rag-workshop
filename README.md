# 🤖 Agentic RAG Workshop (Local + Observability)

Учебный проект по созданию AI-агента технической поддержки, работающего полностью локально (без отправки данных в облако).

Проект демонстрирует современные паттерны построения агентских систем:
1.  **RAG (Retrieval-Augmented Generation)** через LlamaIndex (оптимизированный Retriever).
2.  **Agentic Logic (ReAct)** через LangGraph (память, инструменты).
3.  **Observability (Monitoring)** через Langfuse (трейсинг цепочек, дебаг, токены).
4.  **Local Inference** через Ollama (с поддержкой GPU).

## 🚀 Технологический стек

*   **Мозг (Logic):** [LangGraph](https://langchain-ai.github.io/langgraph/) (Stateful Agent, Memory).
*   **Знания (RAG):** [LlamaIndex](https://www.llamaindex.ai/) (Data Ingestion, Vector Store).
*   **Мониторинг:** [Langfuse](https://langfuse.com/) (Self-hosted via Docker).
*   **LLM & Embeddings:** [Ollama](https://ollama.com/) (Model: `qwen2.5:7b`, Embeddings: `nomic-embed-text`).
*   **Инфраструктура:** Docker Compose (Ollama, Postgres, Clickhouse, Minio, Redis).
*   **Package Manager:** `uv`.

## 🛠 Установка и запуск

### 1. Предварительные требования
*   Python 3.11+
*   Docker & Docker Compose
*   NVIDIA GPU (рекомендуется) + NVIDIA Container Toolkit

### 2. Установка зависимостей
Проект использует современный менеджер пакетов `uv`.

```bash
# Установка uv (если нет)
pip install uv

# Создание окружения и установка библиотек
uv sync
```

### 3. Запуск инфраструктуры
Мы используем Docker для запуска Ollama и платформы Langfuse.

**Шаг A. Поднятие контейнеров**
```bash
docker compose up -d
```
*Примечание: Первый запуск может занять время, пока инициализируются базы данных и Minio.*

**Шаг B. Загрузка моделей в Ollama**
Выполните команды для скачивания моделей внутрь контейнера:

```bash
# Основная LLM (Qwen3 8B - мощная и быстрая)
docker exec -it ollama ollama pull qwen3:8b

# Эмбеддинг-модель (Nomic - для векторизации документов)
docker exec -it ollama ollama pull nomic-embed-text
```

### 4. Настройка Langfuse (Ключи)
Чтобы агент мог отправлять трейсы, нужно создать проект.

1.  Откройте в браузере: **http://localhost:3000**
2.  Создайте аккаунт (Sign up) — данные хранятся локально.
3.  Создайте организацию и новый проект (New Project).
4.  Зайдите в **Settings -> API Keys** и создайте новые ключи.

### 5. Настройка переменных окружения
Создайте файл `.env` в корне проекта и вставьте туда ключи из шага 4:

```ini
# Файл .env
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_HOST="http://localhost:3000"

```

### 6. Запуск Агента
```bash
uv run main.py
```

## 📂 Структура проекта

*   `main.py` — Точка входа. Граф агента, инициализация Langfuse и чат-луп.
*   `rag_engine.py` — Модуль LlamaIndex. Оптимизированный поиск (Retriever) без лишних LLM-вызовов.
*   `data/` — Папка с документами для базы знаний.
*   `docker-compose.yaml` — Полная конфигурация инфраструктуры (LLM + Observability stack).
*   `.env` — Ключи доступа (не коммитить!).

## 📊 Мониторинг (Trace)
После диалога с агентом, зайдите в Langfuse (**http://localhost:3000**), раздел **Tracing**.
Вы увидите:
*   Полное дерево выполнения (Graph -> Agent -> Tools).
*   Входные и выходные данные на каждом шаге.
*   Точное время выполнения поиска в базе (Latency).
*   Стоимость (Token usage) — для локальных моделей $0, но кол-во токенов считается.

## 📝 Пример работы (Лог сессии)

Ниже приведен реальный пример диалога с агентом. Обратите внимание на сообщения `[Поиск в базе]` — это момент, когда агент решает использовать RAG, и на то, как агент помнит контекст во втором вопросе.

```text
Вы: Привет, кто ты?
⏳ Агент думает...

🤖 Ассистент:
Привет! Я ассистент техподдержки, который помогает найти информацию о технических проблемах, настройках SSO, оплате, API и других вопросах в нашей базе знаний. Чем могу помочь? 😊

Вы: У меня ошибка 429 в API, что делать?
⏳ Агент думает... [Поиск в базе] 

🤖 Ассистент:
Ошибка 429 ("Too Many Requests") означает, что ваш API-клиент отправляет запросы быстрее, чем разрешено лимитом. Вот как это исправить:

1. **Экспоненциальный backoff**  
   Добавьте задержки между запросами (например: 0.5с, 1с, 2с, максимум 30с) с рандомизацией (джиттером), чтобы избежать пиковых нагрузок.

2. **Пул аутентификационных ключей**  
   Используйте несколько API-ключей и чередуйте их в многопоточном режиме.

3. **Проверяйте заголовки**  
   Следите за `X-RateLimit-Remaining` и `Retry-After`, чтобы адаптировать частоту запросов.

4. **Увеличьте квоты (если нужно)**  
   Отправьте в поддержку анализ вашей нагрузки (объём и паттерн запросов) для возможного увеличения лимитов.

Если проблема сохраняется — уточните детали в техподдержке. 😊

Вы: А какие заголовки надо проверить?
⏳ Агент думает...

🤖 Ассистент:
Для корректной обработки лимитов запросов в API обычно проверяют следующие заголовки (названия могут отличаться в зависимости от провайдера):

### 🔑 Основные заголовки:
1. **`X-RateLimit-Remaining`**  
   Показывает, сколько ещё запросов можно отправить до достижения лимита.  
   *Пример:* `X-RateLimit-Remaining: 5` — осталось 5 запросов.

2. **`Retry-After`**  
   Указывает, через сколько секунд можно повторить запрос после временного блокировки.  
   *Пример:* `Retry-After: 30` — ждать 30 секунд.

3. **`X-RateLimit-Limit`** (опционально)  
   Общее количество запросов в текущем периоде (например, за минуту).  
   *Пример:* `X-RateLimit-Limit: 100`.

4. **`X-RateLimit-Reset`** (опционально)  
   Время, когда лимит восстановится (в формате Unix timestamp или UTC).  
   *Пример:* `X-RateLimit-Reset: 1625648400`.

5. **`X-RateLimit-Used`** (опционально)  
   Количество уже отправленных запросов в текущем периоде.  
   *Пример:* `X-RateLimit-Used: 95`.

---

### 📌 Важно:
- Названия заголовков могут отличаться (например, `X-Request-Limit-Remaining` или `RateLimit-Remaining`). Проверьте документацию вашего API.
- Если заголовков нет, используйте **экспоненциальный backoff** и учитывайте частоту запросов вручную.
- Если проблема сохраняется, уточните в техподдержке, какие именно заголовки используются в вашем случае. 😊

Вы: q
До свидания!
```